# Skillet backend

FastAPI backend for Skillet — the AI engineer's cookbook. See the root repo's
`docs/` for the product intent, capability map, and per-module specs.

## Setup

```
uv sync
uv run fastapi dev src/skillet/api/app.py
```

## Test

```
uv run pytest
```
